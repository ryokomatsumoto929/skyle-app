# NewApp.tsx リファクタリング完了ガイド

## 🎯 リファクタリングの目的

700 行以上あった NewApp.tsx を、適切にコンポーネント分割して保守性を向上。

## 📁 新しいファイル構造

```
frontend/src/
├── types/
│   └── index.ts                  # 型定義（120行）
├── constants/
│   └── gradients.ts              # グラデーション定義（25行）
├── hooks/
│   └── useTimeGradient.ts        # 時間帯管理フック（30行）
├── utils/
│   └── timeUtils.ts              # ユーティリティ関数（150行）
├── components/
│   ├── MomentIcon.tsx            # アイコン表示（70行）
│   ├── Header.tsx                # ヘッダー（60行）
│   ├── LocationButton.tsx        # 位置情報ボタン（60行）
│   ├── MomentCard.tsx            # メインカード（180行）
│   ├── NavigationMenu.tsx        # ナビゲーションメニュー（120行）
│   └── AboutPage.tsx             # アバウトページ（30行）
└── NewApp.tsx                    # メインコンポーネント（200行）
```

## 📊 Before / After

### Before（リファクタリング前）

```
NewApp.tsx: 700+ 行
- 全てが1ファイルに詰まっている
- 見通しが悪い
- 再利用できない
```

### After（リファクタリング後）

```
11ファイルに分割
- 各ファイル30〜200行程度
- 役割が明確
- テストしやすい
- 再利用可能
```

## 🔧 適用手順

### 1. ディレクトリ作成

```bash
cd ~/skyle/frontend/src

# 新しいディレクトリを作成
mkdir -p types constants hooks utils components
```

### 2. ダウンロードしたファイルを配置

ダウンロードしたファイルを以下のように配置：

```bash
# types/index.ts
types_index.ts → types/index.ts

# constants/gradients.ts
constants_gradients.ts → constants/gradients.ts

# hooks/useTimeGradient.ts
hooks_useTimeGradient.ts → hooks/useTimeGradient.ts

# utils/timeUtils.ts
utils_timeUtils.ts → utils/timeUtils.ts

# components/*.tsx
components_MomentIcon.tsx → components/MomentIcon.tsx
components_Header.tsx → components/Header.tsx
components_LocationButton.tsx → components/LocationButton.tsx
components_MomentCard.tsx → components/MomentCard.tsx
components_NavigationMenu.tsx → components/NavigationMenu.tsx
components_AboutPage.tsx → components/AboutPage.tsx

# NewApp.tsx
NewApp_refactored.tsx → NewApp.tsx（既存を上書き）
```

### 3. ターミナルでの一括配置コマンド

```bash
cd ~/skyle/frontend/src

# types
mv ~/Downloads/types_index.ts types/index.ts

# constants
mv ~/Downloads/constants_gradients.ts constants/gradients.ts

# hooks
mv ~/Downloads/hooks_useTimeGradient.ts hooks/useTimeGradient.ts

# utils
mv ~/Downloads/utils_timeUtils.ts utils/timeUtils.ts

# components
mv ~/Downloads/components_MomentIcon.tsx components/MomentIcon.tsx
mv ~/Downloads/components_Header.tsx components/Header.tsx
mv ~/Downloads/components_LocationButton.tsx components/LocationButton.tsx
mv ~/Downloads/components_MomentCard.tsx components/MomentCard.tsx
mv ~/Downloads/components_NavigationMenu.tsx components/NavigationMenu.tsx
mv ~/Downloads/components_AboutPage.tsx components/AboutPage.tsx

# NewApp.tsx（バックアップを取ってから）
cp NewApp.tsx NewApp_backup.tsx
mv ~/Downloads/NewApp_refactored.tsx NewApp.tsx
```

### 4. 動作確認

```bash
# フロントエンド起動
cd ~/skyle/frontend
npm run dev

# ブラウザで確認
# http://localhost:5173
```

## ✅ リファクタリングの利点

### 1. **可読性の向上**

- 各ファイルが 30〜200 行程度で読みやすい
- ファイル名から役割が一目瞭然

### 2. **保守性の向上**

- 修正箇所が明確
- バグの影響範囲が限定的

### 3. **再利用性**

- MomentIcon、Header、LocationButton などは他でも使える
- カスタムフック（useTimeGradient）も再利用可能

### 4. **テスタビリティ**

- 各コンポーネントを独立してテスト可能
- ユーティリティ関数のユニットテストが容易

### 5. **チーム開発**

- 担当箇所が明確
- 並行作業がしやすい

## 🎨 各ファイルの役割

### types/index.ts

- 全ての型定義を一元管理
- TypeScript の型安全性を確保

### constants/gradients.ts

- グラデーション配色の定義
- プリロード画像リスト
- 配色変更時はここだけ修正

### hooks/useTimeGradient.ts

- 時間帯に応じたグラデーション管理
- デモモード対応
- 自動更新ロジック

### utils/timeUtils.ts

- 時間帯判定関数
- 時刻フォーマット関数
- 次のモーメント検索ロジック
- ビジネスロジックの集約

### components/MomentIcon.tsx

- マジックアワー/ブルーモーメントのアイコン表示
- 画像読み込み管理
- フェードイン効果

### components/Header.tsx

- ヘッダー表示（ロゴ＋メニューボタン）
- デモモード表示

### components/LocationButton.tsx

- 位置情報取得ボタン
- 座標表示

### components/MomentCard.tsx

- メインのカード表示
- 時刻・天気情報
- ハロ予報

### components/NavigationMenu.tsx

- メニュー表示
- ページ切り替え
- 時間帯プレビュー

### components/AboutPage.tsx

- アバウトページ

### NewApp.tsx（メイン）

- 全体の統括
- State 管理
- API 呼び出し
- コンポーネントの組み立て

## 🚀 今後の拡張

### 簡単に追加できるもの

- 新しいページ（components/NewPage.tsx）
- 新しい時間帯（constants/gradients.ts）
- 新しいフック（hooks/useXxx.ts）

### 例：新しいページ追加

```typescript
// components/SettingsPage.tsx を作成
import React from "react";
export const SettingsPage = () => { ... };

// NewApp.tsx に追加
{currentPage === "settings" && <SettingsPage />}

// NavigationMenu.tsx に追加
<MenuItem onClick={() => handlePageChange("settings")}>
  設定
</MenuItem>
```

## 📝 Git 管理

```bash
cd ~/skyle

# 新しいファイルを追加
git add frontend/src/types/
git add frontend/src/constants/
git add frontend/src/hooks/
git add frontend/src/utils/
git add frontend/src/components/
git add frontend/src/NewApp.tsx

# コミット
git commit -m "refactor: NewApp.tsxをコンポーネント分割

- 700行以上のコードを11ファイルに分割
- types, constants, hooks, utils, components に整理
- 保守性・再利用性・テスタビリティを向上
- 各ファイル30〜200行で可読性向上"

# プッシュ
git push origin main
```

---

**リファクタリング完了日**: 2025 年 11 月 21 日  
**目的**: コードの保守性向上・チーム開発準備  
**成果**: 700+ 行 → 11 ファイル（平均 100 行）
